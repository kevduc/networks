export default class Network {
    constructor() {

    }

    static connect(device1, device2) {
        console.log("Connect", device1, device2);
        //device1.connect(device2);
        //device2.connect(device1);
    }
}